<h1 class="ProductIndexHeader">All Products</h1>
<div class="row">
<% @items.each do |item| %>
  <div class="col-lg-3">
    <img src="<%= item.image %>" alt="">
    <li><%= item.title %></li>
    <%= item.description %>
    <%= item.price %><br /><br />
  </div>
<% end %>
</div>
